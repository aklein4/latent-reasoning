
model_type: swift
architectures: [
    SwiftModel
]

max_sequence_length: 1024

hidden_size: 1024
mlp_size: 1536

attention_head_size: 64
num_attention_heads: 8

num_layers: 48

hidden_act: silu
norm_eps: 0.00001

rope_fraction: 2
rope_base: 10000

z_size: 256
z_over_scale: 5

gradient_checkpointing: false
